# Stack

## Runtime
- Static browser runtime: HTML, CSS, and JavaScript loaded by `index.html`.
- No server-side runtime or application backend is present in the repository.
- Recommended local preview: `python -m http.server 8000` from the repository root.

## Languages
- HTML: `index.html`.
- CSS: `assets/css/style.css`.
- JavaScript: `assets/script/main.js`.
- Page language: Portuguese Brazil, declared as `lang="pt-br"`.

## Frameworks and dependencies
- No frontend framework is configured.
- No package manifest, lockfile, bundler, or dependency manager is present.
- Poppins is loaded from Google Fonts by `index.html`.
- No other externally verified runtime dependency is configured.

## Build and deployment
- No build step is defined.
- No CI workflow, container configuration, or deployment configuration is present.
- The source files are intended to be served as static files.

## Unknowns
- [TODO] Confirm the production hosting target and browser support policy.
- [TODO] Confirm whether the external Google Fonts request is permitted in the deployment environment.

## Evidence
- `index.html`
- `assets/css/style.css`
- `assets/script/main.js`
- `README.md`
- `docs/codebase/.codebase-scan.txt`
